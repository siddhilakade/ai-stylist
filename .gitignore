# Secrets — never commit
.env
.env.example
.streamlit/secrets.toml
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Dataset cache (raw HF parquet files are large; regenerate with scripts/build_catalog.py)
.cache/
data/raw/

# OS / editor / tooling
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# The trained model IS committed: it is a few MB and the app must never retrain
# at page load. Anything larger should be regenerated instead.
models/*.joblib.tmp

# Assessment source material (not part of the deliverable)
*.pdf
